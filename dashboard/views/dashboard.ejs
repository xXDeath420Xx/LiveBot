<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Server Selection - CertiFried Announcer</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/dashboard.css?v=1.0.1">
</head>
<body>

    <main class="container">
        <div class="header-card">
            <h1>Server Selection</h1>
            <p>Choose a server where you have Administrator permissions to manage.</p>
        </div>
        <div class="server-grid">
            <% (manageableGuilds || []).forEach(guild => { %>
                <a href="/manage/<%= guild.id %>" class="server-card-link">
                    <div class="server-card">
                        <% if (guild.icon) { %>
                            <img src="https://cdn.discordapp.com/icons/<%= guild.id %>/<%= guild.icon %>.png?size=128" alt="<%= guild.name %> Icon">
                        <% } else { %>
                            <img src="/images/default-icon.png" alt="<%= guild.name %> Icon">
                        <% } %>
                        <h2><%= guild.name %></h2>
                        <span>Click to manage</span>
                    </div>
                </a>
            <% }); %>
        </div>
    </main>

</body>
</html>