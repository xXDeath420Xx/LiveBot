<!DOCTYPE html>
<html lang="en" data-bs-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - CertiFried MultiTool</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <link rel="stylesheet" href="/css/unified-dark-theme.css">
    <style>
        /* Enhanced Hero Section */
        .dashboard-hero {
            text-align: center;
            padding: 60px 30px;
            background: linear-gradient(135deg, #0a0e27 0%, #1e2028 50%, #16181d 100%);
            border-radius: 24px;
            margin-bottom: 48px;
            position: relative;
            overflow: hidden;
        }

        .dashboard-hero::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: radial-gradient(circle at 30% 50%, rgba(88, 101, 242, 0.15) 0%, transparent 50%),
                        radial-gradient(circle at 70% 50%, rgba(155, 89, 182, 0.15) 0%, transparent 50%);
            pointer-events: none;
        }

        .dashboard-hero-content {
            position: relative;
            z-index: 2;
        }

        .user-avatar {
            width: 128px;
            height: 128px;
            border: 4px solid rgba(88, 101, 242, 0.3);
            box-shadow: 0 16px 48px rgba(88, 101, 242, 0.4);
            margin-bottom: 24px;
            transition: all 0.3s ease;
            animation: avatarFloat 4s ease-in-out infinite;
        }

        .user-avatar:hover {
            transform: scale(1.08);
            border-color: rgba(88, 101, 242, 0.6);
            box-shadow: 0 20px 60px rgba(88, 101, 242, 0.6);
        }

        @keyframes avatarFloat {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(-12px); }
        }

        .dashboard-hero h1 {
            font-size: 2.8rem;
            font-weight: 800;
            background: linear-gradient(135deg, #ffffff 0%, #b9bbbe 50%, #5865F2 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            margin-bottom: 12px;
        }

        .dashboard-hero p {
            font-size: 1.15rem;
            color: rgba(255, 255, 255, 0.75);
            margin-bottom: 0;
        }

        /* Enhanced Server Cards */
        .server-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
            gap: 24px;
            margin-bottom: 64px;
        }

        .server-card {
            background: rgba(255, 255, 255, 0.04);
            border: 1px solid rgba(88, 101, 242, 0.2);
            border-radius: 16px;
            padding: 24px;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            cursor: pointer;
            backdrop-filter: blur(10px);
            position: relative;
            overflow: hidden;
        }

        .server-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 3px;
            background: linear-gradient(90deg, #5865F2 0%, #b959f6 100%);
            transform: scaleX(0);
            transform-origin: left;
            transition: transform 0.3s ease;
        }

        .server-card:hover {
            transform: translateY(-8px);
            background: rgba(255, 255, 255, 0.08);
            border-color: rgba(88, 101, 242, 0.5);
            box-shadow: 0 20px 48px rgba(88, 101, 242, 0.25);
        }

        .server-card:hover::before {
            transform: scaleX(1);
        }

        .server-icon {
            width: 64px;
            height: 64px;
            border-radius: 14px;
            border: 2px solid rgba(88, 101, 242, 0.2);
            transition: all 0.3s ease;
            box-shadow: 0 8px 24px rgba(0, 0, 0, 0.3);
        }

        .server-card:hover .server-icon {
            border-color: rgba(88, 101, 242, 0.5);
            box-shadow: 0 12px 32px rgba(88, 101, 242, 0.4);
        }

        .server-name {
            font-size: 1.125rem;
            font-weight: 600;
            color: #ffffff;
            margin: 0;
            line-height: 1.4;
        }

        .server-arrow {
            color: rgba(255, 255, 255, 0.4);
            font-size: 1.25rem;
            transition: all 0.3s ease;
        }

        .server-card:hover .server-arrow {
            color: #5865F2;
            transform: translateX(4px);
        }

        /* Bottom Stats Section */
        .dashboard-stats {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
            gap: 24px;
            margin-top: 48px;
        }

        .stat-box {
            background: rgba(255, 255, 255, 0.04);
            border: 1px solid rgba(88, 101, 242, 0.2);
            border-radius: 16px;
            padding: 32px 24px;
            text-align: center;
            transition: all 0.3s ease;
            backdrop-filter: blur(10px);
        }

        .stat-box:hover {
            background: rgba(255, 255, 255, 0.08);
            border-color: rgba(88, 101, 242, 0.4);
            transform: translateY(-6px);
            box-shadow: 0 16px 40px rgba(88, 101, 242, 0.2);
        }

        .stat-icon {
            font-size: 2rem;
            margin-bottom: 16px;
            color: #5865F2;
        }

        .stat-value {
            font-size: 2.25rem;
            font-weight: 800;
            background: linear-gradient(135deg, #5865F2 0%, #b959f6 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            display: block;
            margin-bottom: 8px;
        }

        .stat-label {
            font-size: 0.875rem;
            color: rgba(255, 255, 255, 0.65);
            text-transform: uppercase;
            letter-spacing: 0.05em;
            font-weight: 500;
        }

        /* Empty State */
        .empty-state {
            text-align: center;
            padding: 80px 32px;
            background: rgba(255, 255, 255, 0.04);
            border: 2px dashed rgba(88, 101, 242, 0.3);
            border-radius: 20px;
            margin-top: 32px;
        }

        .empty-state i {
            font-size: 4rem;
            color: rgba(88, 101, 242, 0.3);
            margin-bottom: 24px;
        }

        .empty-state h3 {
            color: rgba(255, 255, 255, 0.9);
            margin-bottom: 12px;
        }

        .empty-state p {
            color: rgba(255, 255, 255, 0.6);
            font-size: 1.05rem;
        }
    </style>
</head>
<body>

<%- include('partials/header', { user: user }) %>

<main class="page-container">
    <!-- Hero Section -->
    <div class="dashboard-hero">
        <div class="dashboard-hero-content">
            <img src="<%= user.avatar ? `https://cdn.discordapp.com/avatars/${user.id}/${user.avatar}.png` : `https://cdn.discordapp.com/embed/avatars/${user.discriminator % 5}.png` %>" alt="User Avatar" class="rounded-circle user-avatar">
            <h1>Welcome, <%= user.username %>!</h1>
            <p>Select a server to manage.</p>
        </div>
    </div>

    <!-- Server Grid -->
    <% if (manageableGuilds.length > 0) { %>
        <div class="server-grid">
            <% manageableGuilds.forEach(guild => { %>
                <a href="/manage/<%= guild.id %>" class="text-decoration-none">
                    <div class="server-card">
                        <div class="d-flex align-items-center">
                            <img src="<%= guild.icon ? `https://cdn.discordapp.com/icons/${guild.id}/${guild.icon}.png` : 'https://cdn.discordapp.com/embed/avatars/0.png' %>" alt="<%= guild.name %>" class="server-icon me-3">
                            <div class="flex-grow-1">
                                <h5 class="server-name"><%= guild.name %></h5>
                            </div>
                            <i class="fas fa-chevron-right server-arrow"></i>
                        </div>
                    </div>
                </a>
            <% }); %>
        </div>

        <!-- Bottom Stats Section -->
        <div class="dashboard-stats">
            <div class="stat-box">
                <i class="fas fa-server stat-icon"></i>
                <div class="stat-value"><%= manageableGuilds.length %></div>
                <div class="stat-label">Active Servers</div>
            </div>
            <div class="stat-box">
                <i class="fas fa-users stat-icon"></i>
                <div class="stat-value"><%= manageableGuilds.reduce((sum, g) => sum + (g.approximate_member_count || 0), 0).toLocaleString() %></div>
                <div class="stat-label">Total Members</div>
            </div>
            <div class="stat-box">
                <i class="fas fa-video stat-icon"></i>
                <div class="stat-value">0</div>
                <div class="stat-label">Live Streams</div>
            </div>
        </div>
    <% } else { %>
        <div class="empty-state">
            <i class="fas fa-server"></i>
            <h3>No Servers Found</h3>
            <p>No manageable servers found. Make sure you have the "Manage Server" permission.</p>
        </div>
    <% } %>
</main>

<%- include('partials/footer') %>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>