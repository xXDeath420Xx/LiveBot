<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0"><title>Server Selection</title>
<link rel="stylesheet" href="/css/dashboard.css">
</head>
<body>
<nav class="navbar">
<a href="/" class="nav-brand"><img src="/images/CertiFried.png" alt="Logo"><span>CertiFried Announcer</span></a>
<div class="user-info"><span>Signed in as <strong><%= user.username %></strong></span><a href="https://certifriedannouncer.online/logout" class="btn-logout">Log Out</a></div>
</nav>
<main class="container">
<div class="header-card"><h1>Server Selection</h1><p>Choose a server where you have Administrator permissions to manage.</p></div>
<div class="server-grid">
<% guilds.forEach(guild => { const botGuild = client.guilds.cache.get(guild.id); if (botGuild) { %>
<a href="/manage/<%= guild.id %>" class="server-card-link">
<div class="server-card">
<img src="<%= botGuild.iconURL({ size: 128, extension: 'png' }) || '/images/default-icon.png' %>" alt="<%= botGuild.name %> Icon">
<h2><%= botGuild.name %></h2><span>Click to manage</span>
</div>
</a>
<% } }); %>
</div>
</main>
</body>
</html>