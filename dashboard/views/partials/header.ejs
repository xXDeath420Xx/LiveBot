<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard | CertiFried Announcer</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <style>
         :root{--bs-body-bg:#36393f;--bs-body-color:#dcddde;--bs-secondary-bg:#2f3136;--bs-tertiary-bg:#202225;--bs-border-color:#40444b;--bs-primary:#5865f2;--bs-primary-rgb:88,101,242;--bs-link-color:#5865f2;--bs-link-hover-color:#4752c4}
         .form-control,.form-select{background-color:var(--bs-tertiary-bg);color:var(--bs-body-color);border-color:var(--bs-border-color)}
         .form-control:focus,.form-select:focus{background-color:var(--bs-tertiary-bg);color:var(--bs-body-color);border-color:var(--bs-primary);box-shadow:0 0 0 .25rem rgba(var(--bs-primary-rgb),.25)}
         .nav-pills .nav-link.active,.nav-pills .show > .nav-link{background-color:var(--bs-primary)}
         .modal-content{background-color:var(--bs-secondary-bg)}
         .sidebar { height: calc(100vh - 56px); position: sticky; top: 56px; }
         .btn-primary { background-color: var(--bs-primary); border-color: var(--bs-primary); }
         .btn-primary:hover { background-color: #4752c4; border-color: #4752c4; }
    </style>
</head>
<body class="bg-dark text-light">
    <nav class="navbar navbar-expand-lg navbar-dark bg-secondary sticky-top">
        <div class="container-fluid">
            <a class="navbar-brand" href="/dashboard">
                <img src="https://cdn.discordapp.com/app-icons/1409904443125665875/e2810a3002f2321487f54c9d54e58b14.png?size=128" alt="Logo" width="30" height="30" class="d-inline-block align-text-top rounded-circle">
                Dashboard
            </a>
            <div class="d-flex align-items-center">
                 <% if (user) { %>
                    <span class="navbar-text me-3 d-none d-md-block">
                        Logged in as <%= user.username %>
                    </span>
                    <a href="/logout" class="btn btn-sm btn-danger">Logout</a>
                 <% } %>
            </div>
        </div>
    </nav>
    <div class="container-fluid">
