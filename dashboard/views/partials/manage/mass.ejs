<div class="row">
    <div class="col-lg-6">
        <div class="content-card h-100">
            <h2><i class="fas fa-file-export me-2"></i>Export Subscriptions</h2>
            <p class="text-muted">Export all individual and team member subscriptions for this server into a single CSV file. This is useful for backups or migrating to another server.</p>
            <hr class="my-4">
            <a href="/manage/<%= guild.id %>/export" class="btn btn-secondary w-100"><i class="fas fa-download me-2"></i>Export All Streamer Subscriptions</a>
        </div>
    </div>
    <div class="col-lg-6">
        <div class="content-card h-100">
            <h2><i class="fas fa-sync-alt me-2"></i>Sync Channel with CSV</h2>
            <p class="text-muted">Adds/updates members from a CSV file for a specific channel. <strong>Warning:</strong> This will remove any streamers from that channel who are not in the file.</p>
            <hr class="my-4">
            <form action="/manage/<%= guild.id %>/import-team" method="POST" enctype="multipart/form-data">
                <div class="mb-3">
                    <label class="form-label">Channel to Sync</label>
                    <select name="channelId" class="form-select" required>
                        <option value="" disabled selected>-- Select a channel --</option>
                        <% channels.forEach(channel => { %>
                            <option value="<%= channel.id %>">#<%= channel.name %></option>
                        <% }) %>
                    </select>
                </div>
                <div class="mb-3">
                    <label class="form-label">CSV File</label>
                    <input class="form-control" type="file" name="csvfile" accept=".csv" required>
                </div>
                <button type="submit" class="btn btn-primary w-100"><i class="fas fa-sync-alt me-2"></i>Sync Channel from CSV</button>
            </form>
        </div>
    </div>
</div>