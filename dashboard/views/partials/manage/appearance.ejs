<div class="content-card">
    <h2><i class="fas fa-paint-brush me-2"></i>Channel Appearance</h2>
    <p class="text-muted">Set a default webhook nickname and avatar for all announcements in a specific channel.</p>
    <hr>
    <form action="/manage/<%= guild.id %>/channel-appearance/save" method="POST">
        <div class="mb-3"><label class="form-label">Channel to Customize</label><select name="channelId" class="form-select" required><option value="" disabled selected>-- Select a channel --</option><% channels.forEach(c => { %><option value="<%= c.id %>">#<%= c.name %></option><% }) %></select></div>
        <div class="mb-3"><label class="form-label">Custom Nickname</label><input type="text" class="form-control" name="nickname" placeholder='Optional Nickname (type "reset" to clear)' maxlength="80"></div>
        <div class="mb-3"><label class="form-label">Custom Avatar URL</label><input type="text" class="form-control" name="avatar_url_text" placeholder='Optional URL (type "reset" to clear)'></div>
        <button type="submit" class="btn btn-primary w-100 mt-3"><i class="fas fa-save me-2"></i>Save Channel Appearance</button>
    </form>
</div>