<style>
/* Clean Professional Modal Styles */
.modal-content {
    background: linear-gradient(135deg, #0a0e27 0%, #1e2028 50%, #16181d 100%);
    border: 1px solid rgba(88, 101, 242, 0.3);
    border-radius: 16px;
    box-shadow: 0 24px 64px rgba(0, 0, 0, 0.5);
}

.modal-header {
    border-bottom: 1px solid rgba(88, 101, 242, 0.2);
    padding: 24px 32px;
    background: rgba(255, 255, 255, 0.02);
}

.modal-title {
    font-size: 1.5rem;
    font-weight: 700;
    background: linear-gradient(135deg, #ffffff 0%, #5865F2 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    display: flex;
    align-items: center;
    gap: 12px;
}

.modal-body {
    padding: 32px;
}

.modal-footer {
    border-top: 1px solid rgba(88, 101, 242, 0.2);
    padding: 20px 32px;
    background: rgba(255, 255, 255, 0.02);
}

.btn-close {
    background: transparent url("data:image/svg+xml,%3csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 16 16' fill='%23fff'%3e%3cpath d='M.293.293a1 1 0 011.414 0L8 6.586 14.293.293a1 1 0 111.414 1.414L9.414 8l6.293 6.293a1 1 0 01-1.414 1.414L8 9.414l-6.293 6.293a1 1 0 01-1.414-1.414L6.586 8 .293 1.707a1 1 0 010-1.414z'/%3e%3c/svg%3e") center/1em auto no-repeat;
    opacity: 0.7;
    transition: opacity 0.2s;
}

.btn-close:hover {
    opacity: 1;
}

.form-label {
    font-size: 0.875rem;
    font-weight: 600;
    color: rgba(255, 255, 255, 0.9);
    margin-bottom: 8px;
    display: block;
}

.form-control, .form-select {
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(88, 101, 242, 0.2);
    border-radius: 8px;
    padding: 12px 16px;
    color: #ffffff;
    font-size: 0.9rem;
    transition: all 0.2s;
}

.form-control:focus, .form-select:focus {
    background: rgba(255, 255, 255, 0.08);
    border-color: rgba(88, 101, 242, 0.5);
    box-shadow: 0 0 0 3px rgba(88, 101, 242, 0.15);
    outline: none;
    color: #ffffff;
}

.form-control::placeholder {
    color: rgba(255, 255, 255, 0.4);
}

.section-divider {
    margin: 32px 0;
    border: 0;
    height: 1px;
    background: linear-gradient(90deg, transparent, rgba(88, 101, 242, 0.3), transparent);
}

.section-header {
    font-size: 1.1rem;
    font-weight: 600;
    color: #ffffff;
    margin-bottom: 8px;
    display: flex;
    align-items: center;
    gap: 10px;
}

.section-description {
    font-size: 0.875rem;
    color: rgba(255, 255, 255, 0.6);
    margin-bottom: 20px;
}

.form-check-input {
    background-color: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(88, 101, 242, 0.3);
    width: 44px;
    height: 24px;
    cursor: pointer;
}

.form-check-input:checked {
    background-color: #5865F2;
    border-color: #5865F2;
}

.form-check-input:focus {
    box-shadow: 0 0 0 3px rgba(88, 101, 242, 0.15);
}

.form-check-label {
    color: rgba(255, 255, 255, 0.8);
    margin-left: 12px;
    font-size: 0.9rem;
}

.btn {
    padding: 12px 24px;
    border-radius: 8px;
    font-weight: 600;
    font-size: 0.9rem;
    transition: all 0.2s;
    border: none;
}

.btn-secondary {
    background: rgba(255, 255, 255, 0.08);
    color: rgba(255, 255, 255, 0.9);
}

.btn-secondary:hover {
    background: rgba(255, 255, 255, 0.12);
    color: #ffffff;
}

.btn-primary {
    background: linear-gradient(135deg, #5865F2 0%, #4752c4 100%);
    color: #ffffff;
}

.btn-primary:hover {
    background: linear-gradient(135deg, #6b77f4 0%, #5865F2 100%);
    transform: translateY(-1px);
    box-shadow: 0 8px 20px rgba(88, 101, 242, 0.3);
}

textarea.form-control {
    resize: vertical;
    min-height: 80px;
}

select.form-select option {
    background: #1e2028;
    color: #ffffff;
}
</style>

<div class="modal fade" id="add-streamer-modal" tabindex="-1" aria-labelledby="addStreamerModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg modal-dialog-scrollable modal-dialog-centered">
        <div class="modal-content">
            <form action="/manage/<%= guild.id %>/add-streamer" method="POST">
                <div class="modal-header">
                    <h5 class="modal-title" id="addStreamerModalLabel">
                        <i class="fas fa-user-plus"></i>
                        Add New Streamer
                    </h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <!-- Basic Information -->
                    <div class="row g-3 mb-4">
                        <div class="col-md-6">
                            <label class="form-label">
                                <i class="fas fa-globe me-1" style="color: #5865F2;"></i>
                                Platform
                            </label>
                            <select name="platform" class="form-select" required>
                                <option value="twitch">Twitch</option>
                                <option value="youtube">YouTube</option>
                                <option value="kick">Kick</option>
                                <option value="tiktok">TikTok</option>
                                <option value="trovo">Trovo</option>
                                <option value="facebook">Facebook Gaming</option>
                                <option value="instagram">Instagram</option>
                            </select>
                        </div>
                        <div class="col-md-6">
                            <label class="form-label">
                                <i class="fas fa-user me-1" style="color: #5865F2;"></i>
                                Username/Channel ID
                            </label>
                            <input type="text" name="username" class="form-control" placeholder="Enter streamer username" required>
                        </div>
                        <div class="col-12">
                            <label class="form-label">
                                <i class="fab fa-discord me-1" style="color: #5865F2;"></i>
                                Link Discord User (Optional)
                            </label>
                            <input type="text" name="discord_user_id" class="form-control" placeholder="Discord User ID (e.g., 123456789012345678)">
                        </div>
                    </div>

                    <hr class="section-divider">

                    <!-- Announcement Settings -->
                    <h5 class="section-header">
                        <i class="fas fa-bullhorn" style="color: #5865F2;"></i>
                        Announcement Settings
                    </h5>
                    <p class="section-description">Configure how and where stream announcements will be posted</p>

                    <div class="row g-3">
                        <div class="col-md-6">
                            <label class="form-label">Announcement Channel(s)</label>
                            <select name="announcement_channel_id" class="form-select" multiple size="4">
                                <option value="">-- Use Server Default --</option>
                                <% channels.forEach(c => { %>
                                    <option value="<%= c.id %>"># <%= c.name %></option>
                                <% }); %>
                            </select>
                            <small style="color: rgba(255, 255, 255, 0.5); font-size: 0.8rem; margin-top: 6px; display: block;">
                                Hold Ctrl/Cmd to select multiple channels
                            </small>
                        </div>
                        <div class="col-md-6">
                            <label class="form-label">Display Name Override</label>
                            <input type="text" class="form-control" name="override_nickname" placeholder="Leave blank to use their username">
                            <small style="color: rgba(255, 255, 255, 0.5); font-size: 0.8rem; margin-top: 6px; display: block;">
                                Custom name shown in announcements
                            </small>
                        </div>
                        <div class="col-12">
                            <label class="form-label">Custom Announcement Message</label>
                            <textarea class="form-control" name="custom_message" rows="3" placeholder="Hey @everyone! {streamer} just went live playing {game}! Check it out at {url}"></textarea>
                            <small style="color: rgba(255, 255, 255, 0.5); font-size: 0.8rem; margin-top: 6px; display: block;">
                                Available variables: {streamer}, {game}, {title}, {url}
                            </small>
                        </div>
                        <div class="col-12">
                            <div class="form-check form-switch">
                                <input class="form-check-input" type="checkbox" id="keep_summary_add" name="keep_summary">
                                <label class="form-check-label" for="keep_summary_add">
                                    Keep announcement after stream ends (don't delete/edit message)
                                </label>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                        <i class="fas fa-times me-2"></i>Cancel
                    </button>
                    <button type="submit" class="btn btn-primary">
                        <i class="fas fa-plus me-2"></i>Add Streamer
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>
