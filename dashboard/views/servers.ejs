<!DOCTYPE html>
<html lang="en" data-bs-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Servers - CertiFried Announcer</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <link rel="stylesheet" href="/css/dashboard-style.css?v=2.1.0">
</head>
<body>

<%- include('partials/header', { user: user }) %>

<main class="page-container">
    <div class="page-header text-center">
        <h1><i class="fas fa-server me-2"></i>Your Servers</h1>
        <p class="lead">Select any server below to configure its settings.</p>
    </div>

    <div class="row g-4">
        <% guilds.forEach(guild => { %>
            <div class="col-md-6 col-lg-4">
                <a href="/manage/<%= guild.id %>" class="text-decoration-none">
                    <div class="content-card server-card h-100">
                        <div class="d-flex align-items-center">
                            <img src="<%= guild.icon ? `https://cdn.discordapp.com/icons/${guild.id}/${guild.icon}.png` : 'https://cdn.discordapp.com/embed/avatars/0.png' %>" alt="<%= guild.name %> Icon" width="60" height="60" class="rounded me-3">
                            <div class="flex-grow-1">
                                <h5 class="mb-1"><%= guild.name %></h5>
                                <small class="text-muted">Manage Settings</small>
                            </div>
                            <i class="fas fa-chevron-right ms-auto text-muted"></i>
                        </div>
                    </div>
                </a>
            </div>
        <% }); %>
    </div>
</main>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
<%- include('partials/footer') %>
</body>
</html>