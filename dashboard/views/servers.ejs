<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Select a Server - CertiFried Announcer</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <link rel="stylesheet" href="/css/dashboard-style.css?v=4.0.0">
</head>
<body>

<%- include('partials/header') %>

<div class="server-selection-container">
    <div class="text-center mb-5">
        <h1 class="section-title">My Servers</h1>
        <p class="text-muted">Select a server to configure.</p>
    </div>

    <% if (guilds && guilds.length > 0) { %>
        <% guilds.forEach(guild => { %>
            <a href="/manage/<%= guild.id %>" class="server-card">
                <img src="https://cdn.discordapp.com/icons/<%= guild.id %>/<%= guild.icon %>.png" class="server-card-icon" alt="<%= guild.name %> icon" onerror="this.src='https://cdn.discordapp.com/embed/avatars/0.png'">
                <div class="server-card-info">
                    <div class="server-card-name"><%= guild.name %></div>
                </div>
                <div class="server-card-manage">Manage</div>
            </a>
        <% }); %>
    <% } else { %>
        <div class="plugin-card">
            <div class="plugin-card-body text-center">
                <p>No manageable servers found. Make sure the bot has been invited and that you have 'Manage Server' permissions.</p>
                <a href="https://discord.com/api/oauth2/authorize?client_id=<%= client_id %>&permissions=8&scope=bot%20applications.commands" class="btn btn-primary" target="_blank">Invite the Bot</a>
            </div>
        </div>
    <% } %>
</div>

<%- include('partials/footer') %>

</body>
</html>