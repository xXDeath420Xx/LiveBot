<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Donate - Support the Bot</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <link rel="stylesheet" href="/css/dashboard-style.css?v=2.0.0">
    <style>
        .donation-card {
            background-color: var(--background-secondary);
            border: 1px solid var(--border-color);
            border-radius: var(--border-radius);
            padding: 2rem;
            text-align: center;
            transition: all 0.2s ease;
        }
        .donation-card:hover {
            transform: translateY(-5px);
            border-color: var(--brand-color);
        }
        .donation-icon {
            font-size: 3rem;
            margin-bottom: 1rem;
        }
        .donation-card h3 {
            color: var(--header-primary);
            margin-bottom: 0.5rem;
        }
        .donation-handle {
            font-family: monospace;
            background-color: var(--background-tertiary);
            padding: 0.5rem;
            border-radius: 5px;
            display: inline-block;
        }
    </style>
</head>
<body>
<%- include('partials/header') %>
<main class="page-container">
    <div class="page-header text-center">
        <h1><i class="fas fa-heart text-danger me-2"></i>Support the Bot</h1>
        <p class="lead">Your support helps cover server costs and funds future development.</p>
        <p>If you find this bot useful, please consider making a donation. Every little bit helps keep the project alive and running smoothly. Thank you!</p>
    </div>

    <div class="row g-4 justify-content-center">
        <div class="col-md-5">
            <div class="donation-card">
                <div class="donation-icon" style="color: #00D632;"><i class="fas fa-dollar-sign"></i></div>
                <h3>Cash App</h3>
                <p>You can send a donation via Cash App to the handle below.</p>
                <p class="donation-handle">$DeathC</p>
                <a href="https://cash.app/$DeathC" class="btn btn-success mt-3" target="_blank">Open Cash App</a>
            </div>
        </div>
        <div class="col-md-5">
            <div class="donation-card">
                <div class="donation-icon" style="color: #0070BA;"><i class="fab fa-paypal"></i></div>
                <h3>PayPal</h3>
                <p>You can send a donation via PayPal using the link below.</p>
                <p class="donation-handle">@xXDeath420Xx</p>
                <a href="https://paypal.me/xXDeath420Xx" class="btn btn-primary mt-3" target="_blank">Open PayPal</a>
            </div>
        </div>
    </div>
</main>
<%- include('partials/footer') %>
</body>
</html>